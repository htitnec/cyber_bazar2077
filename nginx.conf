server {
    listen 80;
    server_name localhost;

    location /hack {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /hack.html;
    }

    location /transfer {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /hack_transfer.html;
    }
}